numpy==1.24.4
opencv-python==4.8.1.78
mediapipe==0.10.14
face-recognition==1.3.0
dlib-bin
pyrealsense2==2.55.1.6486
supabase==2.15.2

# WebRTC — streaming de video en tiempo real
aiortc>=1.9.0
av>=12.0.0

# Opcional: solo hace falta con MOTOR_EMBEDDING=onnx en .env.
# Con el motor por defecto (dlib) el sistema funciona sin esto instalado.
# Medido en el equipo del edge: 217.5 ms por rostro con dlib, 3.2 ms con onnx.
# onnxruntime
